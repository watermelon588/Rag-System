# In development, leave this BLANK — the Vite dev server proxies /api to the
# backend (see vite.config.js), keeping auth cookies first-party.
# In production, set it to the backend origin (which must serve cookies with
# SameSite=None; Secure over HTTPS), e.g. https://api.example.com
VITE_API_BASE_URL=
