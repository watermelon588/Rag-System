/* ═══════════════════════════════════════════════════════════════════════
   GALLERY KNOBS — tune the landing-page gallery from this one file.
   ═══════════════════════════════════════════════════════════════════════ */
export const GALLERY = {
    cardSize: 240,        // px — width/height of each card
    spread: 148,          // px — horizontal distance between neighbouring cards
    hoverPush: 170,       // px — how far siblings slide away on hover
    containerHeight: 290, // px — vertical room for the fanned cards
    maxTilt: 5,           // deg — rotation of the outermost cards

    /* Vertical rhythm of the section (px). Lower `paddingTop` to sit the
       gallery closer to the hero/search box above it. */
    paddingTop: 32,
    paddingBottom: 88,
};
